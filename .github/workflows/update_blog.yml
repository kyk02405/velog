name: Update Blog Posts

on:
  push:
    branches:
      - main
  schedule:
    - cron: '0 * * * *'   # 매 시간 정각 실행
  workflow_dispatch:

jobs:
  update_blog:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.x'

      - name: Install dependencies
        run: pip install feedparser

      - name: Run script
        run: python scripts/update_blog.py

      - name: Commit & Push changes (only if changed)
        run: |
          git config --global user.name "kyk02405"
          git config --global user.email "kyk02405@gmail.com"

          git add .

          # 🔥 변경 없으면 커밋/푸시 안 하고 종료
          if git diff --cached --quiet; then
            echo "No changes detected → No commit, no push, no grass 🌱"
            exit 0
          fi

          git commit -m "🪴 Update Velog posts on $(date '+%Y-%m-%d %H:%M:%S')"
          git push https://${{ secrets.GH_PAT }}@github.com/kyk02405/velog.git
